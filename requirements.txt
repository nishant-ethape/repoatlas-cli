# Core (always required) — no database server needed
typer>=0.12
rich>=13
httpx>=0.27
chromadb>=0.6

# Optional: cloud providers (uncomment to use)
# openai>=1.30
# anthropic>=0.28
# groq>=0.9

# Dev / testing
pytest>=8
pytest-cov
